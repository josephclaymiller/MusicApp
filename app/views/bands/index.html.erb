<h1>Bands</h1>
<% unless @bands.nil? %>
	<ul>
	<% @bands.each do |band| %>
		<li>
			<%= link_to(band.name, band_url(band)) %>
		</li>
	<% end %>
	</ul>
<% end %>

<%= link_to("Create new band", new_band_url) %>

